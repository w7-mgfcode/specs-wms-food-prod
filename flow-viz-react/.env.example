# Mode: mock | supabase | postgres
VITE_DB_MODE=mock

# For Supabase (Required if VITE_DB_MODE=supabase)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# For Self-Hosted PostgreSQL (Required if VITE_DB_MODE=postgres)
# Note: Requires backend API middleware. Frontend cannot connect directly.
VITE_POSTGRES_URL=postgresql://user:pass@localhost:5432/flowviz
